# Component Conventions
 
- Store reusable component files in `pages/components/`.
- Keep top-level page objects in `pages/`.
- Check existing files in both folders before deciding whether new code is a page object or a component.
