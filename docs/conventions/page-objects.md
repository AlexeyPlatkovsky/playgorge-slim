# Page Object Conventions
 
- Store page-object files in `pages/`.
- Store reusable components in `pages/components/`, not beside top-level page objects.
- Store executable specs in `tests/`, not in `pages/`.
